{
  "gateway": {
    "replicas": 3,
    "autoscaling": { "enabled": true, "minReplicas": 3, "maxReplicas": 12 },
    "podDisruptionBudget": { "enabled": true, "minAvailable": 2 },
    "topologySpread": true,
    "zoneWhenUnsatisfiable": "DoNotSchedule",
    "maxUnavailable": 0,
    "maxSurge": 1
  },
  "redis": { "managedArchitecture": "replication", "replicas": 3, "sentinel": true },
  "minimumFailureDomains": 3,
  "guarantees": {
    "availability": "Gateway replicas are spread across three failure domains; managed Redis uses replication and Sentinel.",
    "upgrades": "Rolling updates preserve at least two ready gateway replicas when cluster capacity is sufficient.",
    "durability": "Redis persistence and replication support failover; Pub/Sub events remain ephemeral."
  }
}
