# Cluster configuration

Environment-scoped Kubernetes, Traefik, and MetalLB configuration is delivered by PBV7-487 and PBV7-488. Resource names must use the Propago `<environment>-<application>` convention.
